@page "/services"
@model PetHotelManager.Pages.Services.IndexModel
@{
    ViewData["Title"] = "Quản lý Dịch vụ";
}

<div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center">
    <h1>@ViewData["Title"]</h1>
    <div class="btn-toolbar mb-2 mb-md-0">
        <a asp-page="Create" class="btn btn-sm btn-outline-primary">
            <i class="bi bi-plus-circle-fill"></i>
            Thêm mới Dịch vụ
        </a>
    </div>
</div>

<div class="table-responsive">
    <table class="table table-striped table-sm">
        <thead>
        <tr>
            <th scope="col">Tên Dịch vụ</th>
            <th scope="col">Loại</th>
            <th scope="col" class="text-end">Giá</th>
            <th scope="col">Đơn vị</th>
            <th scope="col" class="text-end">Hành động</th>
        </tr>
        </thead>
        <tbody>
        @foreach (var service in Model.ServiceList)
        {
            <tr>
                <td>@service.Name</td>
                <td>@service.Category</td>
                <td class="text-end">@service.Price.ToString("N0")</td>
                <td>@service.Unit</td>
                <td class="text-end">
                    <form method="post" class="d-inline">
                        <a asp-page="./Edit" asp-route-id="@service.Id" class="btn btn-sm btn-outline-secondary">Sửa</a>
                        <button type="submit" asp-page-handler="Delete" asp-route-id="@service.Id" class="btn btn-sm btn-outline-danger" onclick="return confirm('Bạn có chắc chắn muốn xóa dịch vụ này?')">Xóa</button>
                    </form>
                </td>
            </tr>
        }
        </tbody>
    </table>
</div>